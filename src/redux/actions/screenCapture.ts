/* eslint-disable prettier/prettier */
import { createAction } from '@reduxjs/toolkit';

export const START_SCREEN_CAPTURE_SERVICE = 'START_SCREEN_CAPTURE_SERVICE';

export const startScreenCaptureService = createAction(
  START_SCREEN_CAPTURE_SERVICE
);
