/* eslint-disable prettier/prettier */
export enum ErrorMessages {
  DIRECORY_NOT_FOUND = 'No such directory found',
  FILE_NOT_FOUND = 'File does not exists',
  UPLOADS_CACHING_FAILED = 'Something went wrong while chaching uploading files, there is problem while reading/writing data from uploads directory',
  REMOVE_CACHE_FAILED = 'Something went wrong while removing cache file for upload, there is a problem while reading/writing data from uploads directory',
}

export enum SuccessMessages {}
